# PASO 1: Dependencias básicas de infraestructura
fastapi==0.109.0
uvicorn[standard]==0.27.0
redis==5.0.1
pydantic==2.10.6
pydantic-settings==2.7.1
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.28.1

# PASO 2: Dependencias RAG
chromadb==0.4.22
sentence-transformers==2.3.1
langchain==0.1.4
langchain-community==0.0.14
pypdf==4.0.0
python-docx==1.1.0
python-multipart==0.0.6

# PASO 3: Dependencias Claude API
anthropic==0.120.2

# PASO 4: Dependencias de Autenticación JWT
python-jose[cryptography]==3.3.0
# bcrypt ya viene con chromadb, usamos directamente sin passlib

# PASO 5: QR Code para canal web
qrcode[pil]==7.4.2

# PASO 6: PWA Push Notifications (VAPID)
pywebpush==2.0.0
py-vapid==1.9.1

# PASO 7: devbout-oauth se instala en el Dockerfile via --mount=type=secret,id=github_token

# PASO 8: PostgreSQL (SQLAlchemy async + Alembic) — ver ADR-006 en docs/dev/DECISIONS.md
sqlalchemy==2.0.46
asyncpg==0.31.0
alembic==1.18.4
psycopg2-binary==2.9.11
# PASO 9: Push notifications nativas (FCM/APNs) — ver ADR-007 en docs/dev/DECISIONS.md
firebase-admin==6.0.0
# aioapns (no apns2): apns2 depende de hyper==0.7.0, que fija h2/hyperframe en
# versiones pre-Python-3.10 (usan collections.MutableSet, removido en 3.10+).
# httpcore importa h2 incondicionalmente al crear CUALQUIER httpx.Client(),
# así que esa combinación rompe toda la app (Claude/DeepSeek incluidos), no
# solo el envío de push iOS.
aioapns==4.0
