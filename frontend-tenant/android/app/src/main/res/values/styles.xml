<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Legacy (pre-Android 12): background completo horneado en drawable/splash.png -->
        <item name="android:background">@drawable/splash</item>
        <!-- Android 12+ (androidx.core.splashscreen): la API del sistema ignora
             android:background y solo respeta estos atributos — fondo navy
             sólido + el owl mark (mismo layer que ic_launcher_foreground,
             ya con el padding de safe-zone correcto) centrado encima. -->
        <item name="windowSplashScreenBackground">@color/ius_navy</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>