// Capacitor: tenant-config.js stub.
// En Docker/Nginx, docker-entrypoint.sh regenera este archivo con el TENANT_ID real.
// En Capacitor (app nativa), el tenant se resuelve post-login desde user.tenant_id
// via TenantContext.setTenantId(). Este stub evita el 404 en el <script src> del index.html.
window.__TENANT_CONFIG__ = { tenantId: "" };
